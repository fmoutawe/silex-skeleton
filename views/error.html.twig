<!DOCTYPE html>
<html lang="fr">
<head>
	<meta charset="UTF-8">
	<title></title>
	<style>
		html, body { margin: 0;	padding: 0; }
		body { margin: 0 auto; width: 960px; }
	</style>
</head>
<body>
	<h1 >Error {{ code }}</h1>
	{% if message is defined %}<p>{{ message }}</p>{% endif %}
	<p>Please contact administrator or go back to <a href="{{ app.request.baseUrl }}" >front page</a></p>
</body>
</html>

